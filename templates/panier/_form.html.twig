{{ form_start(form) }}

<table>
    <tr>
        <td>{{ form_label(form.id) }}</td>
        <td>{{ form_widget(form.id) }}</td>
    </tr>
    <tr>
        <td>{{ form_label(form.idOffre) }}</td>
        <td>{{ form_widget(form.idOffre) }}</td>
    </tr>
    <tr>
        <td>{{ form_label(form.datePanier) }}</td>
        <td>{{ form_widget(form.datePanier) }}</td>
    </tr>
</table>

<button class="btn">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
