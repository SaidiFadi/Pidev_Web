<form method="post" action="{{ path('app_reservation_delete', {'idbillet': reservation.idbillet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.idbillet) }}">
    <button class="btn">Delete</button>
</form>
