{% extends 'base.html.twig' %}

{% block title %}Logement{% endblock %}

{% block body %}
    <h1>Logement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlogement</th>
                <td>{{ logement.idlogement }}</td>
            </tr>
            <tr>
                <th>Adrl</th>
                <td>{{ logement.adrl }}</td>
            </tr>
            <tr>
                <th>Superfice</th>
                <td>{{ logement.superfice }}</td>
            </tr>
            <tr>
                <th>Loyer</th>
                <td>{{ logement.loyer }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ logement.type }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ logement.region }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ logement.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_logement_index') }}">back to list</a>

    <a href="{{ path('app_logement_edit', {'idlogement': logement.idlogement}) }}">edit</a>

    {{ include('logement/_delete_form.html.twig') }}
{% endblock %}
