<form method="post" action="{{ path('app_logement_delete', {'idlogement': logement.idlogement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logement.idlogement) }}">
    <button class="btn btn-danger">Delete</button>
</form>