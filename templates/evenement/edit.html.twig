{% extends 'base.html.twig' %}

{% block title %}Modifier l'Evenement{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">Modifier cet évènement</h1>

        {{ include('evenement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="text-center mt-3">
            <a href="{{ path('app_evenement_indexAdmin') }}" class="btn btn-light">Retour à l'accueil</a>
            {{ include('evenement/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
