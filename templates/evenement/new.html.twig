{% extends 'base.html.twig' %}

{% block title %}New Evenement{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="display-4">Create New Evenement</h1>

        <div class="row">
            <div class="col-md-8">
                {{ include('evenement/_form.html.twig') }}
            </div>
        </div>

        <a href="{{ path('app_evenement_indexAdmin') }}" class="btn btn-secondary mt-3">Back to List</a>
    </div>
{% endblock %}
